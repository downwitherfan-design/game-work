# دستور کار فوری — «دُردانه باید یک اپ واقعی شود»

> **وضعیت:** فعال و الزامی. این سند بر همه‌ی برنامه‌های قبلی اولویت دارد.
> **صادرکننده:** مهندس پروژه (AI یکپارچه‌ساز)
> **مخاطب:** مهندس + مدیر گرافیک — پروژه دو AI فعال دارد.
> **مبنا:** بازخورد مستقیم مالک محصول پس از نصب APK نسخه‌ی ۱.۰.۰ روی گوشی واقعی.

---

## بخش ۰ — چه اتفاقی افتاد؟ (تشخیص، نه سرزنش)

APK ساخته شد، امضا شد، نصب شد — و **کار نمی‌کرد**. مالک محصول این جمله را نوشت:

> «بازی رو نصب کردم و واقعا آشغال و مزخرف بود! … این همه تلاش منتهی به این اپ مسخره شده؟»
>
> «بازی باید یه منوی اصلی، یه تنظیمات یه مراحل یه اینجوری چیزا داشته باشه، **شبیه یه اپ واقعی**»

نکته‌ی دردناک: **۱۱۲۱ تست سبز بود.** هر ۱۵ پکیج تست خودش را پاس می‌کرد. یعنی
مشکل در «کیفیت پکیج‌ها» نبود؛ مشکل در **یکپارچه‌سازی** بود. سه باگ ریشه‌ای:

| # | باگ | چرا تست‌ها نگرفتند؟ |
|---|-----|---------------------|
| ۱ | `app-shell` هرگز `engine`/`audio`/`bus` را به `GameScreen` تزریق نمی‌کرد → `game-board` به Mock داخلی خودش سقوط می‌کرد | هر پکیج Mock خودش را تست می‌کرد، هیچ‌کس «اتصال» را تست نمی‌کرد |
| ۲ | `Onboarding` مستقیماً `MockGameScreen` را import می‌کرد | تست فقط رندر شدن را چک می‌کرد، نه اینکه *کدام* کامپوننت |
| ۳ | CSS دیزاین‌سیستم (`tokens.css` + `ui-kit.css`) و `GAME_BOARD_CSS` هرگز در اپ واقعی تزریق نمی‌شدند (فقط در `demo`) | تست‌های CSS فایل را با `readFileSync` می‌خواندند، نه از مرورگر |

نتیجه: کاربر یک صفحه‌ی سفید با متن «این بخش هنوز آماده نیست» دید.

### 🔴 ضدالگوی اصلی: **Mock-Fallback خاموش**

در سراسر پروژه این الگو تکرار شده است:

```ts
// ❌ ضدالگو — خرابی را پنهان می‌کند
const engine = props.engine ?? resolveEngine();   // اگر تزریق نشود → Mock
const C = mod[name] ?? MockScreen;                 // اگر export نباشد → Mock
```

این الگو **باگ سیم‌کشی را به یک «ویژگی» تبدیل می‌کند**: اپ خراب نمی‌شود، فقط
بی‌سر‌و‌صدا آشغال می‌شود. **از این لحظه ممنوع است** (بخش ۲، قانون ق-۱).

---

## بخش ۱ — چه چیزی تا این لحظه انجام شده (مهندس)

این کارها **انجام شده و روی `main` است**؛ دوباره انجام ندهید:

- ✅ مرج PR #7 (بازطراحی بصری سبک آمیرزا — AI-04)
- ✅ **تزریق سرویس‌های واقعی** در `app-shell`: `GameRoute` حالا `engine`/`audio`/`bus` را از `useShell()` پاس می‌دهد
- ✅ `Onboarding` به `GameScreen` واقعی وصل شد
- ✅ `bootstrap.tsx`: `tokens.css` + `ui-kit.css` import شد و `GAME_BOARD_CSS` در `<head>` تزریق می‌شود
- ✅ **صفحه‌ی خانه (`/`)**: منوی اصلی واقعی با ۶ کاشی + ۳ آمار زنده از Storage
- ✅ **صفحه‌ی مراحل (`/levels`)**: ۴۸ مرحله در ۴ دفتر، قفل/باز‌شدن پیش‌رونده، پیشرفت پایدار
- ✅ `GameScreen` پنج prop اختیاری گرفت: `onFinished`, `practiceSeed`, `practiceDifficulty`, `hideDifficultyPicker`, `hideNewPractice`
- ✅ **رفع نامرئی‌بودن کاشی‌ها**: `.gb-board` تخته‌ی چوبی تیره گرفت (قبلاً `rgba(0,0,0,0.05)` بود و کاشی کِرِم روی پس‌زمینه‌ی کِرِم گم می‌شد)
- ✅ **رفع سرریز افقی برد** و **رفع پنهان‌شدن ردیف سوم کیبورد زیر ناوبری**
- ✅ رفع کنتراست WCAG AA دکمه‌ها (`--dor-btn-bg` ۲.۷۳ → ۴.۷۸؛ `--dor-btn-secondary-fg` ۳.۲۸ → ۶.۵۰)
- ✅ رفع باگ پارسر تست کنتراست (سلکتور گروهی) که ۳۳ تست را الکی می‌شکست
- ✅ ۲۱ تست جدید برای سیستم مراحل

---

## بخش ۲ — مرز مالکیت: دو AI، نه پانزده

پروژه در عمل **دو** AI فعال دارد. تقسیم کار پانزده‌نفره‌ی
`docs/00_MASTER_PLAN.md` تاریخی است و برای مالکیت فایل **دیگر معتبر نیست**.

| نقش | مالک | قلمرو فایل |
|-----|------|-----------|
| **مدیر گرافیک** | AI طراح | `packages/ui-kit/**` — توکن‌ها، رنگ، بافت، آیکون، فونت، انیمیشن، کامپوننت‌های بصری پایه |
| **مهندس** (این AI) | AI یکپارچه‌ساز | **هر چیز دیگری**: `app-shell`, `game-board`, `core-engine`, `word-db`, `meta-retention`, `culture-cards`, `duel-mode`, `monetization`, `analytics`, `audio-haptics`, `viral-share`, `backend-api`, `contracts`, `qa`, `native`, `.github`, `tools`, `docs` |

### قانون مرز (ق-۴ — بازنویسی‌شده)

| قلمرو | مالک | یعنی چه |
|-------|------|---------|
| **ظاهر** | مدیر گرافیک | *چه رنگی، چه بافتی، چه حسی* — `--dor-*` و `.dor-*` در ui-kit |
| **چیدمان و منطق** | مهندس | *چه چیزی کجا و چقدر جا می‌شود، و چه کار می‌کند* |

قواعد عملی:

1. **مهندس hex نمی‌نویسد.** هر رنگ باید `var(--dor-*)` باشد. اگر توکن لازم
   وجود ندارد، مهندس آن را **موقتاً** با fallback مصرف می‌کند
   (`var(--dor-x, <مقدار موقت>)`) و در این سند ثبت می‌کند تا مدیر گرافیک
   توکن رسمی را اضافه کند.
2. **مدیر گرافیک چیدمان را عوض نمی‌کند** — `min-block-size`, `flex`,
   `grid-template`, `aspect-ratio`, `overflow` و متغیرهای `--dor-shell-chrome`
   / `--gb-*` قلمرو مهندس‌اند.
3. **هیچ override دور‌زننده‌ای.** اگر یک استایل در پکیجی غلط است و آن پکیج
   مالِ مهندس است، **در منبع** درست می‌شود، نه با override در `shell.css`.
   (نمونه‌ی واقعی: `.mr-card` رنگ `--dor-bg` داشت و کارت‌های آمار نامرئی
   بودند؛ درست‌کردن آن در `kit-adapter.tsx` انجام شد، نه با override.)
4. **hex در تست قفل نمی‌شود.** تست باید *قاعده* را بسنجد («کنتراست ≥ ۴.۵»)
   نه *مقدار* را. قفل‌کردن hex باعث شد بازطراحی معتبر مدیر گرافیک ۳۳ تست را
   بشکند — آن تست‌ها اصلاح شدند.

### فایل‌های مشترک با ریسک تعارض

| فایل | وضعیت |
|------|-------|
| `packages/ui-kit/src/tokens.css` | مدیر گرافیک. مهندس فقط در صورت شکست WCAG AA دست می‌زند و در کامنت دلیل می‌نویسد. |
| `packages/ui-kit/src/ui-kit.css` | مدیر گرافیک. |
| `packages/game-board/src/styles.ts` | **مهندس.** چیدمان بازی این‌جاست. مدیر گرافیک قبل از هر تغییری `git pull` کند. |
| `packages/app-shell/src/app/shell.css` | **مهندس.** |

---

## بخش ۳ — دستور کار

> `P0` = بلوکه‌کننده‌ی انتشار · `P1` = نسخه‌ی بعدی · `P2` = بهبود

---

### 🎨 مدیر گرافیک — `packages/ui-kit/**`

کار شما در PR #7 **پذیرفته و مرج شد** و پایه‌ی بصری فعلی است. اما این‌ها مانده:

- **P0 — تعارضی که باید بدانید:** من مجبور شدم در
  `packages/game-board/src/styles.ts` دست ببرم (سه باگ چیدمانی: کاشی نامرئی،
  سرریز افقی، پنهان‌شدن ردیف سوم کیبورد). **هیچ توکن یا رنگی را تغییر ندادم.**
  آن فایل قلمرو مهندس است؛ قبل از کار روی آن `git pull` کنید.
- **P0 — کاشی سیال.** `.dor-tile` اندازه‌ی **ثابت** `56px` +
  `min-inline-size: 48px` دارد. داخل گرید سیالِ برد این سرریز می‌ساخت و من
  ناچار شدم در `game-board` override کنم. لطفاً واریانت رسمی اضافه کنید:
  ```css
  .dor-tile--fluid { inline-size:100%; block-size:100%; min-inline-size:0; min-block-size:0; }
  ```
  تا override حذف شود.
- **P0 — `.dor-primary` وجود ندارد.** ui-kit فقط `.dor-btn--primary` دارد.
  پوسته از `.dor-primary` استفاده می‌کرد و دکمه‌ها **خام** رندر می‌شدند. من
  موقتاً سبک گنبدی‌اش را در `shell.css` نوشتم. یا alias رسمی اضافه کنید، یا
  بگویید تا پوسته را به `.dor-btn--primary` مهاجرت دهم.
- **P0 — کنتراست WCAG AA.** دو توکن را اصلاح کردم چون تست AA را می‌شکستند:
  | توکن | قبل | بعد | کنتراست |
  |------|-----|-----|---------|
  | `--dor-btn-bg` | `#ec7823` | `#b84f0c` | ۲.۷۳ → ۴.۷۸ |
  | `--dor-btn-secondary-fg` | `#4d8f2c` | `#2f5c16` | ۳.۲۸ → ۶.۵۰ |

  **رنگ دلخواه شما آزاد است** — فقط شرط `≥ 4.5` برای متن حفظ شود و
  `npm test -w @dordaneh/ui-kit` سبز بماند.
- **P1 — توکن‌های گمشده‌ای که من با fallback مصرف کردم** (لطفاً رسمی کنید):
  | توکن درخواستی | کاربرد | fallback فعلی |
  |---|---|---|
  | `--dor-level-done` | مرحله‌ی تمام‌شده | مشتق از `--dor-correct` |
  | `--dor-level-current` | مرحله‌ی جاری | مشتق از `--dor-gold` |
  | `--dor-level-locked` | مرحله‌ی قفل | مشتق از `--dor-absent` |
  | `--dor-card-paper` | کارت کاغذی (آمار/گنجینه) | `--dor-surface` |
- **P1 — آیکون واقعی جای ایموجی.** منوی اصلی از 🌟🪜🎯📜📊⚙️ استفاده می‌کند؛
  در گوشی‌های مختلف متفاوت رندر می‌شود و «اپ واقعی» به نظر نمی‌رسد.
- **P1 — تم تیره** روی صفحه‌های جدید (خانه، مراحل) بازبینی نشده است.
- **P2 — فونت display.** `--dor-font-display` همان Vazirmatn است. اگر فونت
  تیتر متمایزی می‌خواهید، اضافه کنید (زیرمجموعه‌ی فعلی ۲۰۰ گلیف است و
  ارقام فارسی را دارد — بررسی شد).

---

### 🔧 مهندس (این AI) — بقیه‌ی کدبیس

#### انجام‌شده در این فاز

- رفع سه باگ ریشه‌ای یکپارچه‌سازی (تزریق سرویس، import اشتباه Mock، تزریق CSS)
- صفحه‌ی خانه (`/`) — منوی اصلی + آمار زنده
- صفحه‌ی مراحل (`/levels`) — ۴۸ مرحله‌ی قطعی در ۴ دفتر + ۲۱ تست
- `onFinished` و seed/دشواری قطعی روی `GameScreen`
- چیدمان برد بدون هیچ «تخمین ارتفاع» (flex + aspect-ratio)
- تزریق سرویس `meta` و رفع سفیدی صفحه‌های آمار/گنجینه
- `tools/check-real-app.mjs` — گِیت مرورگری «اپ واقعی»
- رفع باگ پارسر تست کنتراست + تبدیل تست‌های قفل‌کننده‌ی hex به تست قاعده

#### باقی‌مانده

- **P0** گِیت «اپ واقعی» را به `ci.yml` و **قبل از** ساخت APK در
  `apk-debug.yml` / `release.yml` وصل کن. بدون این، فاجعه تکرارشدنی است.
- **P0** `resolve()` در `app-shell/src/app/screens.ts` باید در حالت DEV
  **throw** کند. سقوط خاموش به Mock همان چیزی بود که کاربر دید (ق-۱).
- **P0** `getPracticePuzzle(seed, difficulty)` قطعی است؟ صفحه‌ی مراحل به آن
  تکیه دارد. تست یکپارچه برای هر ۴۸ مرحله بنویس (معما معتبر، بدون تکرار کلمه).
- **P0** بردن مرحله در آمار ثبت نمی‌شود — `recordLevelWin` فقط پیشرفت مراحل
  را ذخیره می‌کند و `gamesPlayed` را دست نمی‌زند.
- **P0** صدا: در اپ نصب‌شده هیچ صدایی نبود. سرویس `audio` حالا تزریق می‌شود؛
  حضور فایل‌ها در باندل Capacitor و سیاست autoplay در WebView باید تست شود.
- **P0** `native/README.md` رانبوک انتشار — بازبینی و تکمیل.
- **P1** «ادامه‌ی بازی»: `getResumableSession()` وجود دارد و در خانه استفاده
  نمی‌شود.
- **P1** دکمه‌ی بازگشت سخت‌افزاری اندروید در صفحه‌ی بازی باید به مراحل برگردد،
  نه خروج از اپ.
- **P1** بازخورد برد مرحله: انیمیشن «مرحله تمام شد» + دکمه‌ی «مرحله‌ی بعد»
  درون خود صفحه‌ی بازی.
- **P1** `GAME_BOARD_CSS` را فایل `.css` واقعی کن تا Vite باندلش کند و
  «فراموش‌شدنِ تزریق» ناممکن شود.
- **P1** رویدادهای آنالیتیکس مراحل: `level_started`, `level_completed`,
  `level_failed`, `chapter_completed`.
- **P1** جایزه‌ی پایان دفتر (کارت فرهنگی) + دستاوردهای مرحله‌ای.
- **P1** ماتریس دستگاه: ۳۶۰×۶۴۰ / ۴۱۲×۹۰۰ / ۴۸۰×۱۰۸۰ — چیدمان `dvh` در
  گوشی‌های کوتاه تست نشده.
- **P2** چانک ۸۰۹KB — code-split.
- **P2** `/duel` و `/shop` پشت فلگ بمانند. **نسخه‌ی اول بدون تبلیغ** منتشر
  می‌شود؛ تا بازی سرگرم‌کننده نشود هیچ مونتیزیشنی اضافه نمی‌شود.

---

## بخش ۴ — تعریف «تمام‌شده» برای این فاز

یک کار تمام است اگر و فقط اگر:

1. ✅ `npm run lint` سبز
2. ✅ `npm test` سبز (همه‌ی ۱۶ سوئیت)
3. ✅ `npm run build:app` بدون خطا
4. ✅ در **مرورگر** روی اپ مونتاژشده دیده و تأیید شده — نه فقط در `demo` پکیج
5. ✅ صفر `.dor-mock-note`، صفر خطای کنسول
6. ✅ در ویوپورت ۴۱۲×۹۰۰ بدون اسکرول ناخواسته و بدون عنصر پنهان زیر ناوبری
7. ✅ `node tools/check-real-app.mjs` سبز — **گِیت «اپ واقعی»**
8. ✅ Conventional Commit و فقط در قلمرو مالکیت خود (بخش ۲)

### گِیت «اپ واقعی» چه چیزی را می‌سنجد؟

```bash
npm run build:app
npx vite preview --port 3000 &
node tools/check-real-app.mjs
```

روی **همه‌ی** مسیرها: صفر Mock · صفر خطای کنسول · صفر کلید ترجمه‌ی خام (همه‌ی
namespaceها) · صفحه‌ی بازی بدون اسکرول · کیبورد پنهان‌نشده زیر ناوبری ·
تایپ با کیبورد روی صفحه واقعاً کار می‌کند.

این گِیت در همان اجرای اول **پنج باگ واقعی** پیدا کرد که ۱۱۴۲ تست واحد
ندیده بودند: دو استثنای صفحه (`getStats`/`getAlbum` روی `undefined`)،
کیبورد پنهان در `/practice`، کلیدهای خام `metaRetention.*`، و کارت‌های
نامرئی آمار. **این دقیقاً دلیل وجودش است.**

---

## بخش ۵ — نقشه‌ی فعلی اپ (منبع حقیقت)

```
/                → خانه: منوی اصلی + ۳ آمار زنده + ۶ کاشی
/daily           → معمای روز
/levels          → ۴۸ مرحله در ۴ دفتر (قفل پیش‌رونده)
/practice        → تمرین آزاد با انتخاب دشواری
/stats           → آمار و استریک
/album           → گنجینه‌ی کارت‌های فرهنگی
/settings        → تنظیمات (تم، کوررنگی، صدا، موسیقی، هپتیک...)
/duel/*          → دوئل (پشت فلگ، در منو نیست)
/shop            → فروشگاه (پشت فلگ، در منو نیست)

همه‌ی این صفحه‌ها از نظر منطق و چیدمان مالِ **مهندس**‌اند؛ زبان بصری‌شان
(رنگ/بافت/آیکون) مالِ **مدیر گرافیک**.
```

ناوبری پایین (۶ مقصد): خانه · روزانه · مراحل · آمار · گنجینه · تنظیمات

### قراردادهای چیدمانی که نباید شکسته شوند

| متغیر | مالک | معنا |
|-------|------|------|
| `--dor-shell-chrome` | مهندس (`shell.css`) | ارتفاع هدر + ناوبری + نوار صفحه. صفحه‌ی بازی از آن کم می‌کند. |
| `--gb-len` / `--gb-rows` | مهندس (`board.tsx`) | طول کلمه و تعداد حدس — تخته با اینها `aspect-ratio` می‌گیرد. |

⚠️ متغیر `--gb-outside` **حذف شد.** یک تخمین ثابت از ارتفاع اجزای غیرِبرد
بود و در `/practice` (که انتخابگر دشواری ۸۲px اضافه می‌کند) اشتباه می‌شد و
کیبورد زیر ناوبری می‌رفت. جایش:

```
.gb-board { flex: 1 1 auto; aspect-ratio: var(--gb-len) / var(--gb-rows); }
```

یعنی برد **هرچه از فضا بماند** را می‌گیرد و مربع‌بودن خانه‌ها از
`aspect-ratio` می‌آید — بدون هیچ عدد حدسی. **این الگو را نشکنید.**

---

## بخش ۶ — سیستم مراحل (مرجع فنی)

`packages/app-shell/src/core/levels.ts`

- ۴۸ مرحله = ۴ دفتر × ۱۲ مرحله
- دشواری: `difficultyForLevel(n)` — از ۱ (مرحله‌ی ۱) تا ۵ (مرحله‌ی ۴۸)، هرگز نزولی نیست
- **قطعیت:** `seedForLevel(n)` با هش عدد صحیح پایدار — مرحله‌ی N روی هر دستگاه
  همان کلمه است. **هیچ‌کس این تابع را تغییر ندهد**؛ تغییرش پیشرفت همه‌ی کاربران را بی‌معنا می‌کند.
- پیشرفت: کلید `dor.shell.levels`، شکل `{ cleared: number }`، **یکنواخت** (هرگز کم نمی‌شود)
- ۲۱ تست در `packages/app-shell/test/levels.test.ts`

---

## بخش ۷ — اولین کاری که هر دو AI باید همین الان بکنند

1. `git pull origin main`
2. `npm install && npm run build:app`
3. `npx vite preview --port 3000` و **اپ را در مرورگر با ویوپورت گوشی (۴۱۲×۹۰۰) باز کنید**
4. `node tools/check-real-app.mjs` — باید سبز باشد
5. همه‌ی مسیرها را کلیک کنید و با چشم خودتان ببینید چه چیزی به کاربر تحویل می‌دهید
6. سپس بندهای `P0` قلمرو خودتان را در بخش ۳ انجام دهید

> **جمله‌ای که باید یادمان بماند:** تست سبز به معنی اپ سالم نیست.
> اپ سالم یعنی روی گوشی، در دست کاربر، کار می‌کند.
