{
  "name": "@dordaneh/word-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run validate && vitest run --coverage",
    "validate": "vitest run --no-coverage test/validate.test.ts"
  },
  "dependencies": {
    "@dordaneh/contracts": "*"
  }
}
